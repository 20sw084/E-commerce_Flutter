/*
import 'package:flutter/material.dart';

final categoryNameTextController = TextEditingController();

Future<String?> openDialog({required int newId}) => showDialog<String>(
  context: context,
  builder: (context) => AlertDialog(
    title: const Text('Edit Category'),
    content: TextField(
      onChanged: (val) => ,
      autofocus: true,
      decoration: const InputDecoration(hintText: 'Enter Name Here'),
      controller: categoryNameTextController,
    ),
    actions: [
      ElevatedButton(
        onPressed: () {
          // TODO
          categoryNameTextController.clear();
        },
        child: const Text('Save'),
      ),
    ],
  ),
);
*/
